// A Java program to find the index of a specific element in an integer array

import java.util.*;

public class indexOfElement {
    public static void main(String[] args) {
        Integer arr[] = {10, 20, 30, 40, 50};
        int index = Arrays.asList(arr).indexOf(30);
        System.out.println(index);  
    }
}
